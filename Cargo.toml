[package]
name = "gossipsub-testground"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
testground = "0.2.0"
tokio = { version = "1.19.2", features = ["macros"] }
